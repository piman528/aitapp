class UnivNotice {
  UnivNotice(this.sender, this.title, this.content, this.sendAt);
  // 発信者
  final String sender;
  // タイトル
  final String title;
  // 内容
  final List<String> content;
  // 送信日時
  final String sendAt;
}
