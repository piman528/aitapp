import 'package:flutter/material.dart';

class SyllabusDetail extends StatelessWidget {
  const SyllabusDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('詳細'),
      ),
      body: const Text('うんこ'),
    );
  }
}
